<div class="fixed top-0 left-0 w-screen h-screen bg-black opacity-30">
</div>
<div class="fixed top-1/2 left-1/2 -translate-y-1/2 -translate-x-1/2 md:w-2/3 w-full border border-black rounded-lg p-8 bg-white">
  <h1 class="font-bold text-4xl">
    <%= header_text %>
  </h1>

  <%= yield %>

  <%= link_to 'Return', return_path, class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
</div>
